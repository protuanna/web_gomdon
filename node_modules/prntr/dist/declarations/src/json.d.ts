import { IJsonConfig } from './types';
export declare type ExtendedJsonConfig = IJsonConfig & {
    frameId: string;
};
declare function json(config: ExtendedJsonConfig, printFrame: HTMLIFrameElement): void;
export { json };
