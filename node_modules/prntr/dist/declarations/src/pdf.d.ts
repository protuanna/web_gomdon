import { IPdfConfig } from './types';
export declare type ExtendedIPdfConfig = IPdfConfig & {
    frameId: string;
};
declare function pdf(config: ExtendedIPdfConfig, printFrame: HTMLIFrameElement): void;
export { pdf };
